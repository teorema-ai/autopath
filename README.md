# env
```
    cd $CAUSAL
    conda env create -f causal-env.yml 
    conda activate causal

    cd $DATABLOCKS
    pip install -e .

    cd $SLIDEFLOW
    git checkout teorema/gigapath
    pip install -e .

    cd $PROV_GIGAPATH
    pip install -e .

    cd $CAUSAL_LEARN
    pip install -e .

    cd $CAUSAL
    pip install -e .

```